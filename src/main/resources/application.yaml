spring:
  application:
    name: LotusOJ

  datasource:
    url: jdbc:mysql://localhost:3306/oj_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true